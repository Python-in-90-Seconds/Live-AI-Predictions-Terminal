# Live-AI-Predictions-Terminal

Build an AI model in Python that thinks *live* — right in your terminal.  
No fancy UI. No delays. Just real-time predictions using TensorFlow.

🔧 Requires:
pip install tensorflow numpy
